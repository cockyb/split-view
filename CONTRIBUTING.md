# Split View에 기여하기

버그 제보와 코드 기여를 환영합니다. 이 프로젝트의 목적은 사용자가 직접 선택한 여러 공개 라이브를 로컬 플레이어 창에 배치해 보는 것입니다.

## 개발 환경

Node.js 22 이상과 pnpm을 사용합니다.

```bash
corepack enable
pnpm install --frozen-lockfile
pnpm check
```

개발용 Chrome을 열고 확장을 자동으로 다시 로드하려면 `pnpm dev`를 실행합니다.

## 변경 원칙

- 한 PR은 하나의 문제에 집중하고, 동작 변경에는 테스트를 추가합니다.
- 실제 쿠키, 액세스 토큰, AID, 서명된 manifest/segment URL은 이슈·로그·fixture에 넣지 않습니다.
- 스트리밍 플랫폼의 로고·아이콘·BI를 저장소나 배포물에 추가하지 않습니다.
- 공식 제휴·승인·보증을 암시하는 문구를 사용하지 않습니다.
- 새 호스트 권한은 기능에 꼭 필요한 최소 범위와 사용자 요청 시점이 설명되어야 합니다.
- 플랫폼 계약이 바뀌면 실패를 안전하게 처리하고, 인증 정보 수집이나 제한 우회로 보완하지 않습니다.

다음 기능은 이 프로젝트 범위 밖이며 PR로 받지 않습니다.

- 광고 탐지·차단·건너뛰기
- 로그인, 연령, 구독, 비밀번호, 지역, DRM 또는 권리 제한 우회
- 방송 녹화, 다운로드, 재송출 또는 제3자 제공
- 세션 URL·서명·인증 정보의 저장, 공유 또는 원격 전송
- 플랫폼의 사전 허락이 필요한 브랜드 자산 사용이나 수익화 기능

자세한 근거는 [플랫폼 정책 검토](docs/platform-policy-review.md)를 확인해 주세요.

## 라이선스

기여한 코드는 별도 합의가 없는 한 저장소의 [MIT 라이선스](LICENSE)로 배포되는 것에 동의한 것으로 봅니다. 제3자 코드를 가져올 때는 원 라이선스와 고지를 함께 제출해야 합니다.
